{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MyGame",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/MyGame.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build",
            "MIMode": "gdb",
            "miDebuggerPath": "gdb"
        }
    ]
}